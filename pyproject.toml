[project]
name = "autoglific"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "fastapi>=0.117.1,<1",
  "argon2-cffi>=23.1,<26",
  "cryptography>=43,<47",
  "psycopg[binary]>=3.2,<4",
  "pydantic>=2.11,<3",
  "pydantic-settings>=2.5,<3",
]

[tool.vercel]
entrypoint = "api.index:app"
